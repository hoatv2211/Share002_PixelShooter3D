{
  "permissions": {
    "allow": [
      "Bash(xargs grep -l \"class.*Manager\")",
      "Bash(xargs grep -l \"Singleton\\\\|Instance\")",
      "Bash(chmod +x optimize-images.sh apply-optimizations.sh)"
    ]
  }
}
